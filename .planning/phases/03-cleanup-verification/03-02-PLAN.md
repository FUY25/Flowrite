---
phase: 03-cleanup-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flowrite-marktext/src/main/flowrite/ai/promptBuilder.js
  - flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js
  - flowrite-marktext/test/evals/flowrite-smoke.spec.js
  - .planning/phases/03-cleanup-verification/03-persona-eval.md
autonomous: false
requirements:
  - CLN-02
user_setup:
  - service: ai-gateway
    why: "CLN-02 requires a live persona eval against the real model, and the agent cannot supply the gateway secret."
    env_vars:
      - name: AI_GATEWAY_API_KEY
        source: "Current Flowrite local-development AI gateway credential"
    local_dev:
      - "Expose `AI_GATEWAY_API_KEY` to the shell before running `npm --prefix flowrite-marktext run eval:flowrite`."
must_haves:
  truths:
    - Friendly, critical, and improvement AI review modes produce materially different review tone and content on the same markdown fixture.
    - Any prompt-side tuning stays lightweight and does not expand into the richer persona-design scope reserved for Roadmap Phase 4.
    - A live eval artifact captures the same-document outputs for all three personas so a human can review appropriateness directly.
    - The shipped Have a Look flow still has automated coverage while persona verification is added.
  artifacts:
    - path: flowrite-marktext/src/main/flowrite/ai/promptBuilder.js
      provides: The lightweight persona instruction contract used by AI review requests.
      min_lines: 150
      contains: REVIEW_PERSONA_INSTRUCTIONS
    - path: flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js
      provides: Prompt-contract regression coverage for persona metadata and system text.
      min_lines: 450
      contains: REVIEW_PERSONA_INSTRUCTIONS
    - path: flowrite-marktext/test/evals/flowrite-smoke.spec.js
      provides: Live same-document persona comparison and report generation.
      min_lines: 110
      contains: reflection-draft.md
    - path: .planning/phases/03-cleanup-verification/03-persona-eval.md
      provides: Human-reviewable sample outputs from the live persona eval.
      min_lines: 12
      contains: "## Persona Outputs"
  key_links:
    - from: flowrite-marktext/src/main/flowrite/ai/promptBuilder.js
      to: flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js
      via: runtime request system-text assertions for persona instructions
      pattern: REVIEW_PERSONA_INSTRUCTIONS
    - from: flowrite-marktext/test/evals/flowrite-smoke.spec.js
      to: flowrite-marktext/test/evals/fixtures/reflection-draft.md
      via: same-document live persona comparison fixture
      pattern: reflection-draft
    - from: flowrite-marktext/test/evals/flowrite-smoke.spec.js
      to: .planning/phases/03-cleanup-verification/03-persona-eval.md
      via: markdown report generation for human review
      pattern: 03-persona-eval
---

<objective>
Verify that the three review personas actually feel different on the same document, and minimally harden the contract if they do not.

Purpose: close `CLN-02` with a live-model artifact and human-reviewed judgment, without pre-empting the richer persona-design work scheduled for Phase 4.
Output: lightweight persona-instruction hardening where needed, stronger automated persona checks, and a markdown report that captures friendly, critical, and improvement outputs side by side.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cleanup-verification/03-RESEARCH.md
@flowrite-marktext/src/main/flowrite/ai/promptBuilder.js
@flowrite-marktext/src/main/flowrite/controller.js
@flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js
@flowrite-marktext/test/evals/flowrite-smoke.spec.js
@flowrite-marktext/test/evals/fixtures/reflection-draft.md
@flowrite-marktext/test/e2e/flowrite-ai-review.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the lightweight persona contract only where overlap still exists</name>
  <files>flowrite-marktext/src/main/flowrite/ai/promptBuilder.js, flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js</files>
  <read_first>flowrite-marktext/src/main/flowrite/ai/promptBuilder.js, flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js, .planning/ROADMAP.md, .planning/phases/03-cleanup-verification/03-RESEARCH.md</read_first>
  <action>Review `REVIEW_PERSONA_INSTRUCTIONS` in `flowrite-marktext/src/main/flowrite/ai/promptBuilder.js` and keep the persona contract intentionally lightweight. If the current strings are too overlapping to support the live eval, update them with concrete cues while staying below Phase 4 scope: Friendly should explicitly signal warmth, encouragement, and reflective curiosity; Critical should explicitly signal direct diagnosis of weak reasoning, vagueness, or unsupported leaps; Improvement should explicitly signal actionable next steps, clarity, or structural revision. Keep each persona to one sentence or two short clauses. Do not add examples, anti-pattern lists, or multi-paragraph persona bios. Extend `flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js` so it asserts the exact friendly, critical, and improvement instruction cues that the runtime request injects.</action>
  <verify>npm --prefix flowrite-marktext run unit</verify>
  <acceptance_criteria>
    - `flowrite-marktext/src/main/flowrite/ai/promptBuilder.js` contains `REVIEW_PERSONA_INSTRUCTIONS`.
    - The friendly persona entry contains `warm` or `encouraging`.
    - The critical persona entry contains `direct` or `unsupported leaps`.
    - The improvement persona entry contains `actionable` or `next steps`.
    - `flowrite-marktext/test/unit/specs/flowrite-ai-runtime.spec.js` contains assertions for all three persona instruction texts.
  </acceptance_criteria>
  <done>The prompt-side persona contract is explicit enough to evaluate while still staying inside Phase 3 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the live smoke eval into a same-document persona-distinction proof with a report artifact</name>
  <files>flowrite-marktext/test/evals/flowrite-smoke.spec.js, .planning/phases/03-cleanup-verification/03-persona-eval.md</files>
  <read_first>flowrite-marktext/test/evals/flowrite-smoke.spec.js, flowrite-marktext/test/evals/fixtures/reflection-draft.md, flowrite-marktext/package.json, .planning/ROADMAP.md</read_first>
  <action>Extend `flowrite-marktext/test/evals/flowrite-smoke.spec.js` so the live persona section runs friendly, critical, and improvement AI review requests against the same `reflection-draft.md` fixture, normalizes the returned comment/text content, and asserts the outputs are materially different instead of merely non-empty. Use machine-checkable signals: the three normalized outputs must not all be identical after trimming and whitespace normalization, each outcome must remain comment-first without `propose_suggestion`, and the recorded samples must show at least one wording or focus difference between every persona pair. During the eval, write `.planning/phases/03-cleanup-verification/03-persona-eval.md` with the headings `# Phase 3 Persona Eval`, `## Persona Outputs`, `### Friendly`, `### Critical`, and `### Improvement`, plus fixture name, run date, model, and captured sample output for each persona. If `AI_GATEWAY_API_KEY` is missing, fail the live persona section with the explicit message `AI_GATEWAY_API_KEY is required for CLN-02 live persona verification.` instead of silently skipping the proof.</action>
  <verify>npm --prefix flowrite-marktext run eval:flowrite</verify>
  <acceptance_criteria>
    - `flowrite-marktext/test/evals/flowrite-smoke.spec.js` reads `reflection-draft.md` and runs friendly, critical, and improvement persona requests against it.
    - `flowrite-marktext/test/evals/flowrite-smoke.spec.js` contains assertions that the three normalized persona outputs are not all identical.
    - `flowrite-marktext/test/evals/flowrite-smoke.spec.js` writes `.planning/phases/03-cleanup-verification/03-persona-eval.md`.
    - `.planning/phases/03-cleanup-verification/03-persona-eval.md` contains `## Persona Outputs`, `### Friendly`, `### Critical`, and `### Improvement`.
  </acceptance_criteria>
  <done>The repo has a live-model artifact that captures same-document persona behavior instead of only assuming the prompt strings are enough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated persona contract, automated unit and live eval coverage, and a side-by-side report at `.planning/phases/03-cleanup-verification/03-persona-eval.md`.</what-built>
  <how-to-verify>
    Open `.planning/phases/03-cleanup-verification/03-persona-eval.md` and confirm:
    1. Friendly feedback sounds encouraging and reflective, not blunt.
    2. Critical feedback names weak reasoning or vagueness directly.
    3. Improvement feedback emphasizes concrete next steps or structural refinement.
    4. The three outputs differ in both tone and content focus on the same fixture.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which persona still feels too similar</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix flowrite-marktext run unit` passes
- [ ] `npm --prefix flowrite-marktext run e2e -- test/e2e/flowrite-ai-review.spec.js` passes
- [ ] `npm --prefix flowrite-marktext run eval:flowrite` passes with `AI_GATEWAY_API_KEY` available
- [ ] `.planning/phases/03-cleanup-verification/03-persona-eval.md` captures friendly, critical, and improvement samples side by side
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `CLN-02` is backed by a live same-document persona comparison rather than prompt-only assumptions
- A human reviews the generated artifact and confirms the three persona outputs are appropriately distinct
- Any prompt-side tuning stays intentionally smaller than the richer persona-voice design reserved for Phase 4

</success_criteria>

<output>
After completion, create `.planning/phases/03-cleanup-verification/03-02-SUMMARY.md`
</output>
